import Vue from "vue";
import Vuex from "vuex";
import npmSearch from "@/store/npmSearch";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    npmSearch
  }
});
